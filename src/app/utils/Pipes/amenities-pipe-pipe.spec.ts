import { AmenitiesPipePipe } from './amenities-pipe-pipe';

describe('AmenitiesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AmenitiesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
